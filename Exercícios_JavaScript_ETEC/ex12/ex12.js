var num = prompt("Coloque um número")

var A = 0
var B = 0

if (num < 0) {
    num = B
    confirm(`Seu número é Negativo! Número: ${B}`)
} else {
    num = A
    confirm(`Seu número é Positivo! Número: ${A}`)
}